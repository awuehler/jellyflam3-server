"""Purpose: JellyFlam3 render pipeline and idle-gate package.

Requirements: Sibling modules under ``pipeline/``; configs/jellyflam3.yaml for CLIs.
Usage: ``import pipeline`` or ``python -m pipeline.<module>`` (see ``pipeline.__main__``).
Assumptions: Package version is informational; workers/tools load config via ``pipeline.config``.
"""

__version__ = "0.1.0"
